[phases.setup]
nixPkgs = ["python311", "ffmpeg", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p uploads outputs static"]

[start]
cmd = "python app.py" 